services:
  database:
    image: postgres:14-alpine3.21
    restart: unless-stopped
    expose:
      - 5432:5432
    env_file:
      - ../.env
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
